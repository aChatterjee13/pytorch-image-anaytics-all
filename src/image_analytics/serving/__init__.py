"""Serving & MLOps: ONNX export (+ parity gate), Triton config generation,
embedding extraction, and drift monitoring. Heavy/optional dependencies
(onnx, onnxruntime, faiss, torchserve) are lazy-imported inside each module.
"""
