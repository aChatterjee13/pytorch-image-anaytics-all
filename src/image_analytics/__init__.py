"""image_analytics — modular PyTorch platform for image analytics.

Subpackages:
    core            shared infrastructure (config, registry, trainer, evaluator, callbacks)
    data            datasets, transforms, and samplers
    backbones       feature extractors (timm wrappers, multi-channel stems)
    classification  classification models and training entry points
"""

__version__ = "0.1.0"
