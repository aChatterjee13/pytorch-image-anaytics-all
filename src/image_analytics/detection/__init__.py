"""Object detection: necks, anchors, heads, losses, and training.

From-scratch implementations (FPN, RetinaNet, FCOS, Faster R-CNN, DETR) built
on ``torchvision.ops`` primitives (roi_align, nms, box_iou), plus ecosystem
wrappers (YOLO via ultralytics).
"""
